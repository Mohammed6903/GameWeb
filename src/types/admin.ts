interface QuickStat {
    title: string;
    value: number;
    icon: React.ElementType;
    color: string;
    bg: string;
}
  
interface GameCounts {
    totalCount: number;
    activeCount: number;
}